{
  "name": "cypress-test-automation",
  "version": "1.0.0",
  "description": "This is for Cypress Web Automation",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "cy:dashboard": "npx cypress run --record --key 'add your cypress dashboard key here'"
  },
  "author": "Tayyab Javed",
  "license": "ISC",
  "devDependencies": {
    "@4tw/cypress-drag-drop": "^2.2.3",
    "cypress": "^10.11.0",
    "cypress-mochawesome-reporter": "^3.3.0",
    "cypress-multi-reporters": "^1.6.3"
  },
  "dependencies": {
    "cypress-aiotests-reporter": "^1.0.5",
    "cypress-iframe": "^1.0.1"
  }
}
